# apparmor.d - Full set of apparmor profiles
# Copyright (C) 2024 Alexandre Pujol <alexandre@pujol.io>
# Copyright (C) 2026 Besanon <set508@proton.me>
# SPDX-License-Identifier: GPL-2.0-only

abi <abi/4.0>,

include <tunables/global>

@{exec_path} = @{bin}/lxqt-openssh-askpass
@{att} = ""
profile lxqt-openssh-askpass /{,usr/}bin/lxqt-openssh-askpass {
  include <abstractions/base-strict>
  include <abstractions/bus-accessibility>
  include <abstractions/lxqt>

  @{exec_path}		mr,

  @{bin}/ssh-agent  rPx,
  @{bin}/ssh-add    rix,

  owner @{HOME}/@{XDG_SSH_DIR}/{,*} r,

  owner @{run}/user/@{uid}/ssh-askpass.@{rand6}/{,*} rw,

  owner /tmp/@{int}  		r,

  /dev/tty			rw,

  include if exists <local/lxqt-openssh-askpass>
}

# vim:syntax=apparmor

# vim:syntax=apparmor
